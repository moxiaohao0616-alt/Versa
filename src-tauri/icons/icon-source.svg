<svg width="1024" height="1024" viewBox="0 0 1024 1024" xmlns="http://www.w3.org/2000/svg">
  <!-- Versa macOS-style app icon — light variant.
       macOS Big Sur+ requires the squircle (continuous-corner rounded
       rectangle) to be baked into the icon itself — the OS does NOT mask.
       rx=229 (≈22.4% of 1024) approximates Apple's icon-template radius.
       The V mark is inset to ~70% of the canvas, giving comfortable
       padding from the rounded corners. -->

  <!-- White tile -->
  <rect width="1024" height="1024" rx="229" ry="229" fill="#ffffff" />

  <!-- Left branch — Versa green -->
  <path d="M 308 304 L 512 750"
        stroke="#639922" stroke-width="100"
        stroke-linecap="round" stroke-linejoin="round" fill="none" />
  <circle cx="308" cy="304" r="80" fill="#639922" />

  <!-- Right branch — neutral dark -->
  <path d="M 716 304 L 512 750"
        stroke="#1a1a18" stroke-width="100"
        stroke-linecap="round" stroke-linejoin="round" fill="none" />
  <circle cx="716" cy="304" r="80" fill="#1a1a18" />
</svg>
